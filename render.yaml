services:
  - type: web
    name: html-pdf-api
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: MAX_HTML_BYTES
        value: "2097152"
      - key: PDF_API_KEY
        sync: false
